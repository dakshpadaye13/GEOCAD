# GEOCAD Backend Database Configuration (PostgreSQL)
# DO NOT commit real database passwords or credentials to version control.
DATABASE_URL="postgresql://postgres:postgres_password@localhost:5432/geocad_db?schema=public"
PORT=4000
NODE_ENV="development"
